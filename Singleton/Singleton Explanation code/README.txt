* Each thread return the same object, therefore this is a singleton and it is thread safety
* Once a singleton instance is created, always it will return the same instance & returning process is efficient.